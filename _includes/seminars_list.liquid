<ul>
  {% for entry in site.data.seminars %}
    <li>{% if entry.link %}<a href="{{ entry.link }}" target="_blank" rel="noopener noreferrer">{{ entry.title }}</a>{% else %}{{ entry.title }}{% endif %}</li>
  {% endfor %}
</ul>
