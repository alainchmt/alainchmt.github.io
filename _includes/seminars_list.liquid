<ul class="list-group list-group-flush">
  {% for entry in site.data.seminars %}
    <li class="list-group-item">{% if entry.link %}<a href="{{ entry.link }}" target="_blank" rel="noopener noreferrer">{{ entry.title }}</a>{% else %}{{ entry.title }}{% endif %}</li>
  {% endfor %}
</ul>
